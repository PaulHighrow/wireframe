import { Form, FormTitle, FormBtn, Input } from './LeadForm.styled';

export const LeadForm = () => {
  return (
    <Form>
      <FormTitle>Надішліть нам заявку</FormTitle>
      <Input></Input>
      <Input></Input>
      <FormBtn>Надіслати</FormBtn>
    </Form>
  );
};
